let productItem = document.getElementsByClassName(".grid") //obtenemos la grid de productos
let clonedGrid = grid.cloneNode(true); //clonamos el nodo para obtener la lista de productos
clonedGrid.id = ''
let clonedNode = originalNode.cloneNode(deep);